[pytest]
# Keep generated tool caches and bytecode out of the skill package.
# Prefer: PYTHONDONTWRITEBYTECODE=1 (tests/conftest.py also enforces and cleans).
addopts = -p no:cacheprovider
testpaths = tests
python_files = test_*.py
