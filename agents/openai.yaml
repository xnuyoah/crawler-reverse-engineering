interface:
  display_name: "Crawler Reverse Engineering"
  short_description: "Protocol-first web reversal and Python collector delivery"
  default_prompt: "Use $crawler-reverse-engineering to classify live target, artifact-only, or continuation work, gather sequential paired browser evidence when required, and deliver the smallest protocol-first Python result."

policy:
  allow_implicit_invocation: true
