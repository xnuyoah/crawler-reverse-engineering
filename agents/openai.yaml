interface:
  display_name: "Crawler Reverse Engineering"
  short_description: "Pure-web protocol reverse with signal-driven MCP routing"
  default_prompt: "Use $crawler-reverse-engineering for pure-web protocol reverse only: auto-judge artifact-only vs live target (live-target) vs continuation from signals; default live baseline is chrome-devtools and upgrade to Camoufox/managed host only on fingerprint pressure; use js-reverse after sequential handoff when debug attach exists; keep APK/app/mini-program primary tasks out of scope; deliver the smallest browser-free Python protocol result."

policy:
  allow_implicit_invocation: true
